@page
@model DotnetAPI.Pages.Admin.BatteryPage.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create" class="btn btn-primary">Add Battery</a>
</p>

<div class="row d-flex flex-column align-items-center gap-3">
@foreach (var item in Model.Battery) {
	<div class="col-6 border border-2 rounded p-2">
		<h2>@Html.DisplayFor(modelItem => item.Name)</h2>
		
	</div>
}
</div>

<a asp-page="/Admin/index" class="btn btn-dark">
Admin Dashboard</a>